import 'package:flutter/material.dart';

class InspectionReportFinalize extends StatelessWidget {
  const InspectionReportFinalize({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Inspection Report Finalize'),
      ),
    );
  }
}